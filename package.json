{
  "name": "blog-tokens",
  "version": "1.0.0",
  "description": "A minimal blog with design tokens integration",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "process-tokens": "style-dictionary build --config style-dictionary.config.js",
    "dev": "browser-sync start --server 'src' --files 'src/**/*.html, src/**/*.css' --port 3000 --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "design-tokens",
    "blog",
    "minimal"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@tokens-studio/sd-transforms": "^1.3.0",
    "browser-sync": "^2.29.3",
    "style-dictionary": "^4.3.3"
  }
}
